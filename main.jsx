import React from "react";
import { createRoot } from "react-dom/client";
import DayveAutos from "./dayve-autos.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DayveAutos />
  </React.StrictMode>
);
